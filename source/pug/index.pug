doctype html
html
	head
		include inc/_vars.pug

		meta(chatset='UTF-8')
		title РосКомКоннект – государственный провайдер

		each CSS in ['main']
			link(rel='stylesheet' href=`/assets/css/${CSS}.min.css?v=${project.ver}`)
	body
		aside.panel

		div.game

		each JS in ['kamina', 'core', 'sites', 'init']
			script(src=`/assets/js/${JS}.min.js?v=${project.ver}` defer)
